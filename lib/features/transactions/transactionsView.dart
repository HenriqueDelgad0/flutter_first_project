import 'package:flutter/material.dart';

class TransacationsView extends StatelessWidget {
  const TransacationsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Tela em progresso. Volte aqui mais tarde."),
    );
  }
}